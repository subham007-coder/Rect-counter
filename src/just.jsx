import React from 'react'

function just() {
    return (
        <div>
            <p>make a just component for data send to 1 component to other component</p>
        </div>
    )
}

export default just